import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  root: {
    borderRadius: "50px",
    "& div": {
      borderRadius: "50px",
    },
  },
});

export default useStyles;
