import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  root: {
    "& div": {
      borderRadius: "50px",
      width: "120px",
    },
  },
});

export default useStyles;
